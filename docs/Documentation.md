🔧 Установка и запуск
Предварительные требования

Docker
Docker Compose (опционально)
Запуск проекта

Клонируйте репозиторий:
bash
Copy

git clone https://github.com/yourusername/digital_mentor_dashboard.git
cd digital_mentor_dashboard


Соберите и запустите Docker-контейнер:
bash
Copy

cd backend
docker build -t dashboard-backend .
docker run -p 8000:8000 dashboard-backend


Откройте фронтенд в браузере:
Откройте файл frontend/index.html в вашем браузере.
📂 Бэкенд
config_utils.py
Хранит конфигурационные данные для подключения к API.

      API_KEY
      Ключ API для доступа к "МойКласс"
    
    
      URL_AUTH
      URL для получения токена аутентификации
    
    
      USERS_API_URL
      URL для получения пользователей (пенсионеров)
    
    
      MANAGERS_API_URL
      URL для получения менеджеров (волонтеров)
      
crm_service.py
Содержит функции для работы с API "МойКласс":
get_token_api()
Получает токен для доступа к API.
Метод: POST к URL_AUTH
Параметры:

apiKey: Ключ API из API_KEY
Возвращает: Токен доступа
fetch_all_users()
Получает всех пользователей (пенсионеров) из API.
Метод: GET к USERS_API_URL
Параметры:

limit: Количество записей на странице (100)
offset: Смещение (0, 100, 200, ...)
Возвращает: Список пользователей
fetch_all_managers()
Получает всех менеджеров (волонтеров) из API.
Метод: GET к MANAGERS_API_URL
Параметры:

limit: Количество записей на странице (100)
offset: Смещение (0, 100, 200, ...)
Возвращает: Список менеджеров
data_processing.py
Содержит функции для обработки данных:
get_current_metrics()
Получает текущие метрики.
Использует:

fetch_all_users()
fetch_all_managers()
Возвращает:
json
Copy

{
  "pensioners_count": 0,
  "volunteers_count": 0,
  "last_updated": "2023-01-01T00:00:00",
  "status": "active"
}

get_history_data(period)
Получает исторические данные.
Параметры:

period: Период (7d, 30d, 90d, all)
Использует:

fetch_all_users()
fetch_all_managers()
Возвращает:
json
Copy

{
  "period": "30d",
  "pensioners": [{"date": "2023-01-01", "count": 10}],
  "volunteers": [{"date": "2023-01-01", "count": 5}]
}

dashboard_data.py
Содержит API эндпоинты:
GET /api/dashboard/metrics
Описание: Получение текущих метрик.
Ответ:
json
Copy

{
  "pensioners_count": 0,
  "volunteers_count": 0,
  "last_updated": "2023-01-01T00:00:00",
  "status": "active"
}

GET /api/dashboard/history
Описание: Получение исторических данных.
Параметры:

period: Период (7d, 30d, 90d, all)
Ответ:
json
Copy

{
  "period": "30d",
  "pensioners": [{"date": "2023-01-01", "count": 10}],
  "volunteers": [{"date": "2023-01-01", "count": 5}]
}

main.py
Основной файл FastAPI.
Эндпоинты:

GET / - Приветственное сообщение
Включает роутер из dashboard_data.py
🌐 Фронтенд
api_client.js
Клиент для работы с API.
fetchMetrics()
Получает текущие метрики.
Метод: GET к /api/dashboard/metrics
fetchHistory(period)
Получает исторические данные.
Метод: GET к /api/dashboard/history?period={period}
main.js
Основной JavaScript.
Функции:

updateMetrics() - Обновляет метрики
updateHistoryChart(period) - Обновляет график
renderHistoryChart(history) - Отображает график

